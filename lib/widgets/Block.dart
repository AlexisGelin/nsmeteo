import 'package:flutter/cupertino.dart';

class BlockSmall extends StatelessWidget {
  const BlockSmall({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: 20.0,height: 20,);
  }
}